[project]
name = "da-job-vietnam-telegram-webhook"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx==0.27.0",
    "python-dotenv==1.0.1",
    "loguru==0.7.2",
]

[tool.vercel]
entrypoint = "api.telegram_webhook:handler"

